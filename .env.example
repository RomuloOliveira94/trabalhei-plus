# =====================================================================
# Este arquivo é versionado e serve apenas de MODELO.
# Para credenciais reais, copie para `.env` (que está no .gitignore):
#
#     cp .env.example .env
#     # edite .env com suas credenciais
#
# Nunca commite o `.env` com valores reais.
# =====================================================================

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# Preencha no `.env` (gitignored), NÃO aqui:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=