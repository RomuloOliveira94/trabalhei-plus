<%# The active filter travels as hidden from/to so create/update can keep the
    summary and the append/replace decisions in sync with the list the user
    came from (mirrors the export forms). %>
<%= form_with model: overtime do |f| %>
  <%= hidden_field_tag :from, @filter_start_date %>
  <%= hidden_field_tag :to, @filter_end_date %>

  <% if overtime.errors.any? %>
    <div id="error_explanation" class="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">
      <p class="font-semibold"><%= t(".error_notice", count: overtime.errors.count) %></p>
      <ul class="mt-2 list-inside list-disc">
        <% overtime.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-6 space-y-5">
    <div>
      <%= f.label :start_at, class: "block text-sm font-medium text-gray-700" %>
      <%= f.datetime_local_field :start_at, class: "mt-1 block w-full rounded-md border border-gray-300 bg-white px-3 py-2 text-sm shadow-sm" %>
    </div>
    <div>
      <%= f.label :end_at, class: "block text-sm font-medium text-gray-700" %>
      <%= f.datetime_local_field :end_at, class: "mt-1 block w-full rounded-md border border-gray-300 bg-white px-3 py-2 text-sm shadow-sm" %>
    </div>
    <div>
      <%= f.label :description, class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_area :description, rows: 4, maxlength: 2000, class: "mt-1 block w-full rounded-md border border-gray-300 bg-white px-3 py-2 text-sm shadow-sm" %>
    </div>
    <div class="flex items-center gap-4">
      <%= f.submit t(".submit"), class: "rounded-lg bg-red-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-red-700" %>
      <%# Inside the CRUD modal the dialog-modal controller intercepts this
          link (data-dialog-modal-close) and just closes the dialog; on the
          standalone page it navigates back to the list as before. %>
      <%= link_to t(".cancel"), overtimes_path, data: { dialog_modal_close: true }, class: "text-sm font-medium text-gray-500 hover:text-gray-700" %>
    </div>
  </div>
<% end %>